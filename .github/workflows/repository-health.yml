name: Repository Health

on:
  workflow_dispatch:

# Single declaration. This file previously had `permissions: {}` twice, which is
# a duplicate YAML key — the parser keeps one and silently discards the other.
permissions: {}

jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow availability
        run: echo "Repository workflow scaffold is available."
